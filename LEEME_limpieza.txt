Limpieza tras la revisión — Fintech Quant Lab v0.4.0
=======================================================

ACCIONES MANUALES QUE TIENES QUE HACER EN GITHUB (no hay archivo
que subir para esto, son BORRADOS):

1. Borra estos 3 archivos de app/pages/ (nombres con caracteres
   corruptos, duplicados de las páginas de verdad):
   - app/pages/2_­ƒöù_Cointegraci├│n.py
   - app/pages/4_ÔÜá´©Å_Riesgo.py
   - app/pages/5_­ƒº¬_Backtest.py
   (Puede que en el listado de GitHub se vean con símbolos raros o
   cajitas — es el mismo archivo, bórralo igual aunque el nombre se
   vea distinto al que esperabas.)

2. Borra "launch.bat (Windows)" (con espacio y paréntesis) — ya
   tienes el "launch.bat" correcto en la raíz.

3. Borra LEEME.txt de la raíz del repo (era una nota de la sesión
   de trabajo, no debería quedar commiteada).

ARCHIVOS QUE SÍ SUBES (contenido correcto, listos para sobrescribir):

- Dockerfile          — quita build-essential (innecesario, hay
                         wheels precompiladas) y añade timeout al
                         healthcheck
- CHANGELOG.md          — entrada [0.4.0] con todo lo de esta sesión
- pyproject.toml        — version = "0.4.0"
- __init__.py  -> va en app/__init__.py -- __version__ = "0.4.0"
- app/pages/2_🔗_Cointegración.py  — el contenido bueno (con el
  aviso de look-ahead), en el nombre CORRECTO
- app/pages/4_⚠️_Riesgo.py          — el contenido bueno (con
  Cornish-Fisher), en el nombre CORRECTO
- app/pages/5_🧪_Backtest.py         — el contenido bueno (con
  benchmark_metrics), en el nombre CORRECTO
- tests/test_garch.py   — 5 tests nuevos: converged, GJR-GARCH,
  unidades de conditional_volatility/forecast_volatility

Sobre el punto 7 del análisis que me pegaste (la firma de cache.py):
NO lo he cambiado. Probé revertir `Callable[[F], Any]` a
`Callable[[F], F]` como sugería, y mypy vuelve a fallar:

  error: Incompatible return value type (got "Callable[[Callable[P, R]],
  CachedFunc[P, R]]", expected "Callable[[F], F]")

st.cache_data devuelve un tipo (CachedFunc) que no es literalmente el
mismo tipo F de entrada, aunque se comporte igual en runtime. La firma
actual (Any) es la correcta; no toques ese archivo.

Verificación antes de esta entrega (las 4 capas juntas):
  - python -m compileall -q app/ tests/ notebooks/  -> OK
  - ruff check app/ tests/ notebooks/                -> All checks passed!
  - mypy app/ --ignore-missing-imports               -> Success: no issues found
  - pytest tests/ -v                                 -> 124 passed (antes 119)

Confirmado con `ls app/pages/`: exactamente 8 páginas, sin duplicados.
