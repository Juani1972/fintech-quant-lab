Fintech Quant Lab — Actualización completa
============================================

Este zip mantiene la MISMA estructura de carpetas que el repo
(app/core/, app/pages/, tests/, notebooks/), así que al descomprimirlo
en el móvil verás las carpetas ya organizadas — solo tienes que subir
el contenido de cada carpeta a su carpeta equivalente en GitHub con
"Add file" → "Upload files" (el método que ya te funcionó sin
problemas la vez pasada).

24 archivos modificados/nuevos en total. Resumen por carpeta:

RAÍZ (2 archivos)
  - Dockerfile          — healthcheck sin curl, usa el lockfile,
                           usuario sin privilegios (antes corría como root)
  - launch.bat           — renombrado desde "launch.bat (Windows)"
                           (borra ese archivo viejo en GitHub, si no
                           se queda duplicado)

app/ (1 archivo)
  - config.py             — CACHE_TTL_FACTORS añadido

app/core/ (10 archivos, 1 nuevo)
  - cache.py    [NUEVO]  — desacopla el caché de Streamlit
  - backtest.py           — vectorización O(n²)→O(n) (ronda anterior) +
                             benchmark_metrics (beta, alpha de Jensen,
                             tracking error, information ratio)
  - cointegration.py      — significance parametrizable, fit_until
                             para evitar look-ahead en alpha/beta
  - data_loader.py         — usa app/core/cache.py en vez de streamlit
  - fama_french.py         — usa app/core/cache.py en vez de streamlit
  - garch.py               — converged, rescale_factor (bug de
                              unidades en forecast_volatility corregido),
                              docstring EGARCH aclarado, fix de
                              GJR-GARCH (ronda anterior)
  - optimization.py        — logging en excepciones, deflated_sharpe_ratio,
                              FIX DE BUG: best_params devolvía float en
                              vez de int para parámetros enteros
  - risk.py                 — value_at_risk_cornish_fisher,
                               value_at_risk_filtered_historical
  - robustness.py            — logging en excepciones, pesos del
                                robustness_score parametrizables +
                                robustness_score_weight_sensitivity
  - walkforward.py           — logging en excepciones, parámetro
                                embargo, n_windows_skipped

app/pages/ (3 archivos)
  - 2_🔗_Cointegración.py   — aviso de look-ahead visible en la UI
  - 4_⚠️_Riesgo.py          — VaR Cornish-Fisher añadido a la UI
  - 5_🧪_Backtest.py         — sección de métricas de benchmark añadida

notebooks/ (1 archivo nuevo)
  - 03_walkforward_robustness.py [NUEVO] — de la ronda anterior

tests/ (7 archivos)
  - test_backtest.py       — tests de benchmark_metrics
  - test_cointegration.py  — test_half_life_positive reescrito con un
                              caso de valor conocido (antes era casi
                              imposible de fallar)
  - test_data_loader.py    — 9 tests nuevos con yfinance mockeado
                              (antes load_prices no tenía NINGÚN test)
  - test_optimization.py   — tests del DSR + regresión del bug de
                              best_params (int vs float)
  - test_risk.py            — tests de Cornish-Fisher y FHS
  - test_robustness.py      — tests de pesos y sensibilidad del score
  - test_walkforward.py     — tests del embargo

Verificación antes de la entrega (las 4 capas juntas, sin parches):
  - python -m compileall -q app/ tests/ notebooks/  -> OK
  - ruff check app/ tests/ notebooks/                -> All checks passed!
  - mypy app/ --ignore-missing-imports               -> Success: no issues found
  - pytest tests/ -v                                 -> 119 passed (antes 90)

NOTA sobre el Dockerfile: no he podido levantar el contenedor en este
entorno (no hay Docker disponible aquí), así que el cambio de usuario
no-root está verificado por lectura/patrón estándar, no por build real.
Si algo falla al construir la imagen, dímelo y lo reviso.
